# Database — PostgreSQL connection string
# For local dev: install PostgreSQL, create the database, then run:
#   npm run db:migrate && npm run db:seed
# For Neon: use the pooled connection string with ?sslmode=require
DATABASE_URL="postgresql://postgres@localhost:5432/constellation?schema=public"

# Mapbox GL — required for the map view
# 1. Create a free account at https://account.mapbox.com/auth/signup/
# 2. Copy your "Default public token" from https://account.mapbox.com/access-tokens/
# 3. Paste it below
NEXT_PUBLIC_MAPBOX_TOKEN=

# OpenAI — REQUIRED for AI features (Investment Memo analysis, Strategy extraction/grading)
# 1. Create an account at https://platform.openai.com/
# 2. Generate an API key at https://platform.openai.com/api-keys
# 3. Paste it below
OPENAI_API_KEY=

# Auth.js — Google OAuth
# 1. Create a project at https://console.cloud.google.com/
# 2. Enable "Google+ API" or "People API"
# 3. Create OAuth 2.0 credentials (Web application)
# 4. Add http://localhost:3000/api/auth/callback/google to Authorized redirect URIs
# 5. Run: npx auth secret (to generate AUTH_SECRET)
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Vercel Blob — document storage
# Created automatically when you add Vercel Blob to your project
BLOB_READ_WRITE_TOKEN=

# Sentry — error monitoring (optional for local dev)
SENTRY_DSN=
SENTRY_AUTH_TOKEN=

# Upstash Redis — distributed rate limiting (optional)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
